{
  "name": "mmorpg-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:preview": "npm run build && wrangler pages deploy dist --compatibility-date=2024-09-23"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "@pheralb/toast": "^1.0.0",
    "@tailwindcss/vite": "^4.1.11",
    "astro": "^5.10.2",
    "gsap": "^3.13.0",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.6.0",
    "wrangler": "^4.23.0"
  }
}